<!-- Inserting the main content headings  -->
<!-- And making the title value as our h1 value with an if statement. -->
<% if (title) { %>
<h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<%- messages() %>


<!-- This is the link to call the add classification view page with the form -->
<a href="/inv/add_class" id="add_classification" class="add_item">Add Classification</a>

<!-- This is the link to call the add inventory view page with the form -->
<a href="/inv/add_inv" id="add_inventory" class="add_item">Add Inventory</a>